# 后续 TODO 清单

> 当前交接入口：[当前状态与交接.md](当前状态与交接.md)。
>
> 控制基线：K230 以 `640×480` 保留完整管子视场，内部缩放至 `320×240` 做传统视觉与 UART。图传已改为原始帧 `640×240` 管子带、JPEG `Q80/目标 5 FPS`，等待实机性能验收。真实 1 cm 钢球已完成台架五点首测，当前位置模型采用五点分段插值；相机最终装车后必须重新复验。

## 已完成

- [x] 明确唯一 K230 部署入口为 `k230_code/k230_final.py`。
- [x] 实现传统视觉：自适应亮/暗 Blob、面积/形状/圆度门限、预测 ROI、α-β 跟踪。
- [x] 修正 K230 → Ti 发送量纲：发送 `x_cm_x100`，不是像素误差。
- [x] 实现 UART 帧格式与 Python 协议参考；Ti 骨架包含校验、序号、超时、PD 和回中位保护。
- [x] 实现 PC TCP 接收、显示与有界录像队列；图传断连不应阻塞 K230 UART。
- [x] 完成热点实测与性能优化：从约 8–10 FPS VGA 串行方案优化至全视场下 `23–27 FPS` 控制循环。
- [x] 验证并停止使用当前固件不兼容的双通道 VENC/H.264 路线。
- [x] 完成真实 1 cm 钢球台架首测：五点稳定 `cx=(47.19,102.57,160.47,221.28,270.67)` 对应 `(-10,-5,0,+5,+10 cm)`；面积约 `63..100 px`，稳定窗口 `sx` 约 `0.4..1.3 px`。

## P0：摆管、Ti/MG996 与供电装配（当前主线）

- [ ] 按 [下一阶段装配与联调清单.md](下一阶段装配与联调清单.md) 完成机构、电源、Ti UART/PWM 和无球保护联调。

## P1：最终安装后的真实钢球复验（阻塞电机闭环）

- [ ] **固定相机与完整视场**
  - 保持当前 640×480 全视场安装高度；在 `±8°` 摆角时确认整段管子仍在画面内。
  - 锁紧焦距、支架和光源；不得在水管/管壁添加标记或传感器。
  - 交付：中心、正/负最大摆角截图。

- [ ] **五点像素—厘米标定**
  - 正常未喷涂钢球分别置于 `-10、-5、0、+5、+10 cm`。
  - 每点记录内部 320×240 图像中的 `cx/cy/pixels/quality`，拟合 `x_cm=(cx-ZERO_X_PX)/PX_PER_CM`。
  - 更新 `k230_final.py` 的 `POSITION_CAL_PX/POSITION_CAL_CM`、`PIPE_ROI`、Blob 面积范围；相机最终固定前的现有台架参数不可直接用于比赛。
  - 交付：五点表、拟合残差、最终参数和截图。

- [ ] **识别鲁棒性验收**
  - 无球、强反光、轻振动、短遮挡下测试；无球不得持续发送有效位置。
  - 中心固定 30 s：位置标准差 ≤0.3 cm；轻振动 ≤0.5 cm。
  - 保持 PC 图传开启，确认 K230 `Loop` 不低于 20 FPS。
  - 交付：将结果填入 `测试记录表.csv`。

## P0：Ti / MG996 硬件适配（可与视觉标定并行）

- [ ] **合入控制骨架**
  - 将 `firmware_state_machine_skeleton.c` 合入实际 MSPM0/Ti 工程。
  - 实现 `platform_set_beam_enable(bool)`、`platform_servo_write_us(uint16_t)`。
  - UART RX 中断/DMA 调用 `app_uart_rx_bytes(data,length,now_ms)`；100 Hz 任务调用 `app_update(now_ms,0.01f)`。
  - 交付：可编译 Ti 工程、UART 收包计数、50 Hz PWM 示波器截图。

- [ ] **电源、机构与方向**
  - MG996 使用独立大电流电源；K230/Ti/舵机电源共地。
  - 无球标定中位、上下限、±8° 对应脉宽，确认无顶死。
  - 确认 `SERVO_BALL_DIRECTION`：正控制量必须使钢球向正确方向回正。
  - 交付：脉宽表、安全摆角和方向结论。

- [ ] **故障保护**
  - 分别测试：K230 停止、UART 拔线、校验和错误、连续无球、端部安全区。
  - 每项要求：200 ms 内按限速回中位；端部优先执行向内救球。
  - 交付：每项回中位时间与视频。

## P1：闭环调参与任务联调

- [ ] **静态滚球**
  - `Ki=0`；先逐步增大 `Kp`，出现过冲后增加 `Kd`。
  - `-5、0、+5 cm` 各 10 次：5 s 内稳定，最终误差 ≤0.8 cm。
  - 交付：最终 `Kp/Kd`、成功率、最大误差、失败视频。

- [ ] **动态 AB**
  - 静态验收通过才开放；低速开始。
  - 10 次至少 8 次全程钢球误差 ≤1 cm；失败时先降车速/摆幅并核查视觉延迟。
  - 交付：AB 测试表与视频。

- [ ] **整圈与指定点**
  - 顺序固定：`DYN_LAP_CENTER` 成功后，才进入 `DYN_LAP_TARGET`。
  - 评委指定目标到来后，只改目标厘米值，不改视觉零点或机械限位。

## P1：图传与比赛材料

- [ ] **验收管子细节图传（优先）**
  - 烧录当前 `k230_final.py`，确认启动日志为 `Video pipe ROI (0, 120, 640, 240) -> 640x240 Q80 @5fps`。
  - 连续推流 60 s：保存 PC 画面截图、实际 FPS、录像回放及连续 5 行 K230 性能日志。
  - 通过条件：完整管段与边缘刻度清晰可辨，K230 `Loop ≥20 FPS`，UART 不受图传连接/断开影响。
  - 若 `Loop <20 FPS`，仅先将 `pipe_detail` 的目标从 5 FPS 降至 4 FPS；若源图仍糊，先调焦和改善照明，不要盲目提高 JPEG 质量。
- [ ] 图传仅为显示/录像，确认正式测试中 PC 没有任何回传控制通道。
- [ ] 更新设计报告中的最终结构、通信协议、标定表、保护测试和任务结果。

## 明确不要做

- [ ] 不要部署 `k230_h264_dual_stream_probe.py` 或设置 `VIDEO_BACKEND="h264_hw"`；当前 Yahboom CanMV v1.4.3 不兼容。
- [ ] 不要将旧的 `k230_camera_stream.py`、`k230_mjpeg_hotspot_server.py` 或 YOLO 脚本作为比赛入口。
- [ ] 不要在真实钢球标定完成前接通舵机闭环。
- [ ] 不要提高图传 FPS/分辨率来牺牲位置更新率；优先保护 K230 `Loop`。

## 向下一位 agent 提供的最小信息包

1. 本文件和 `当前状态与交接.md`。
2. `k230_final.py` 当前配置段的完整截图/副本。
3. K230 启动日志与连续 5 行性能日志。
4. 五点标定数据（钢球到位后）。
5. Ti 实际工程路径、PWM 脉宽表、PD 参数和保护测试记录。
